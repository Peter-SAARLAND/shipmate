- name: "Ahoi!"
  hosts: shipmate
  connection: local
  gather_facts: false
  tasks:
    - name: "Check!"
      include: build-environment.yml
      tags:
        - hoist
        - sail
    - name: "Validate!"
      debug:
        msg: "shipmate_providers: {{ shipmate_providers }}"
    - name: "Hoist!"
      include: hoist.yml
      tags:
        - hoist
        - sail
    - name: "Sail!"
      include: "provider-{{ item }}.yml"
      with_items: "{{ shipmate_providers }}"
      tags:
        - sail
    